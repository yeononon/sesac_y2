let mathScore = '77';
let engScore = '88';
let avgScore = (Number(mathScore) + Number(engScore)) / 2;
console.log(avgScore);

for(i=1; i<=20; i++) {
    if(i % 2 = 0) {
        sum += i;
        console.log(i, sum);
    }   
}